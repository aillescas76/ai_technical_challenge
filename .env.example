# Copy this file to `.env` and fill in any secrets locally.

OPENAI_API_KEY=
LLM_BASE_URL=

# Defaults can be overridden as needed.
EMBEDDINGS_MODEL=text-embedding-3-small
LLM_MODEL=gpt-4o-mini
VECTOR_STORE_PATH=data/faiss
LLM_TIMEOUT_SECONDS=30
EMBEDDINGS_TIMEOUT_SECONDS=30
ASK_CACHE_MAX_ITEMS=128
ASK_CACHE_TTL_SECONDS=600

# LangFuse (optional – leave blank if you are not using it)
LANGFUSE_PUBLIC_KEY=
LANGFUSE_SECRET_KEY=
LANGFUSE_HOST=http://localhost:3000
